import type { FC, ReactNode } from 'react'

/**
 * Компонент, отображающий контейнер для всех виджетов (Погода, Карта, Телепрограмма, Эфир, Посещаемое, Автор).
 * @returns {ReactNode} React-компонент, отображающий контейнер для всех виджетов.
 */
const Sidebar: FC<ReactNode> = (): ReactNode => {
  return <div>Sidebar</div>
}

export default Sidebar
