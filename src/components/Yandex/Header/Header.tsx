// import styles from './Header.module.css'

import type { ReactNode } from 'react'

/**
 * Компонент, отображающий верхний левый блок ("Сейчас в СМИ", "Рекомендуем") и верхний правый блок ("Работа над ошибками").
 * @returns {ReactNode} React-компонент.
 */
const Header = (): ReactNode => {
  return <div>Header</div>
}

export default Header
