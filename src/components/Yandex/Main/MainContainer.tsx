import type { FC, ReactNode } from 'react'

/**
 * Основной контейнер, содержащий центральную колонку (поиск и новости) и правую колонку.
 *
 * @returns {ReactNode} React-компонент, отображающий основной контейнер.
 */
const MainContainer: FC<ReactNode> = (): ReactNode => {
  return <div className="main-container">{/* ... */}</div>
}

export default MainContainer
