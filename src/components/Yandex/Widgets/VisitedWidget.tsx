import type { ReactNode } from 'react'

/**
 * Компонент, отображающий список посещенных страниц.
 * @returns {ReactNode} React-компонент, отображающий список посещенных страниц.
 */
const VisitedWidget = (): ReactNode => {
  return <div>VisitedWidget</div>
}

export default VisitedWidget
