import type { ReactNode } from 'react'

/**
 * Компонент, отображающий карту или непонятно чего. Я тут не понял.
 * @returns {ReactNode} React-компонент, отображающий карту или непонятно чего.
 */
const MapWidget = (): ReactNode => {
  return <div>MapWidget</div>
}

export default MapWidget
