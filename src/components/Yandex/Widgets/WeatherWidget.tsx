import type { ReactNode } from 'react'

/**
 * Компонент, отображающий информацию о текущей погоде.
 *
 * @returns {ReactNode} React-компонент, отображающий информацию о текущей погоде.
 */
const WeatherWidget = (): ReactNode => {
  return <div>WeatherWidget</div>
}

export default WeatherWidget
