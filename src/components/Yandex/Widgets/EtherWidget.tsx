import type { ReactNode } from 'react'

/**
 * Компонент, отображающий информацию об эфире. Тут я тоже не совсем понял, что должно быть.
 * @returns {ReactNode} React-компонент, отображающий информацию об эфире.
 */
const EtherWidget = (): ReactNode => {
  return <div>EtherWidget</div>
}

export default EtherWidget
